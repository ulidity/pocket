{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [redbug, pocket]}
]}.

{profiles, [
    {dev, [
        {deps, [
            {redbug, "2.0.2"}
        ]}
    ]}
]}.
